# mat

```
full screen all drop

mat.key //localstorage.getItem(key)
mat.base64 //{} //base64 array
mat.index=[] //name array
mat.dat // create blob url
//
mat.get(name,base64flg)
mat.load(key or json)
mat.save()
mat.dl //${mat.key}.json

mat.start();

//tile url
// ... xyz.x32.png //x32 tile
// xyz.0.png,xyz.1.png,xyz.2.png
//
```
